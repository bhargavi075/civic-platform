# Optional: set if your backend is hosted elsewhere
# If not set, Vite proxy will forward /api to localhost:5000
# ⚠️  Leave commented out for local development — setting this bypasses
#     the Vite proxy and causes CORS errors.
# VITE_API_URL=http://localhost:5000/api
