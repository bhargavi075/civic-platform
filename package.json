{
  "name": "civic-platform",
  "version": "1.0.0",
  "description": "AI-Based Multilingual Civic Issue Reporting and Resolution Platform",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "seed": "cd backend && node seed.js",
    "setup": "npm run install:all && echo 'Setup complete! Run: npm run seed (optional), then start backend and frontend separately'"
  },
  "dependencies": {
    "axios": "^1.14.0"
  }
}
